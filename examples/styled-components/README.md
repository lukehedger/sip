# Sip styled-components Example

## Download

```
curl https://codeload.github.com/lukehedger/sip/tar.gz/master | tar -xz --strip=2 sip-master/examples/styled-components
cd styled-components
```

## Run

```
npm install
npm start
```
